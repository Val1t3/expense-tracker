import 'package:flutter/material.dart';

class DetailsListScreen extends StatelessWidget {
  const DetailsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
